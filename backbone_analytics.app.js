(function(){'use strict';const URL='https://frxfoztyctrvsobbawmo.supabase.co',KEY='sb_publishable_or6yc7T71Hpyajn9ctn1FA_BjsB6LhL',BUCKET='bases-tecnico',FOLDER='atual',NAME='eventos backbone.xlsx',PATH=FOLDER+'/'+NAME,$=id=>document.getElementById(id),M=['Jan','Fev','Mar','Abr','Mai','Jun','Jul','Ago','Set','Out','Nov','Dez'],F={year:['Ano_Abertura','Ano'],month:['Mês','Mes'],ddd:['DDD_Ofensor_OK','DDD Ofensor','DDD'],network:['Tipo de rede','Tipo_de_rede'],tip:['Pontas','Ponta'],cause:['Causa_OK','Causa'],semester:['Semestre'],date:['DT/HR ABERTURA','Data_Abertura'],total:['Total']},FILTERS=[['year','Ano'],['month','Mês'],['ddd','DDD'],['network','Tipo de rede'],['tip','Ponta'],['cause','Causa_OK']];let all=[],rows=[],charts={},sb;const norm=v=>String(v??'').trim(),esc=v=>norm(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),fmt=n=>Number(n||0).toLocaleString('pt-BR'),pf=n=>Math.abs(Number(n||0)).toLocaleString('pt-BR',{maximumFractionDigits:1})+'%';function field(r,k){const keys=Object.keys(r||{});for(const n of F[k]){const x=keys.find(z=>norm(z).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase()===norm(n).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase());if(x!==undefined&&r[x]!==null&&r[x]!=='')return r[x]}return null}const weight=r=>{const n=Number(field(r,'total'));return Number.isFinite(n)&&n>0?n:1},sum=a=>a.reduce((x,r)=>x+weight(r),0);function count(a,k){const m=new Map();a.forEach(r=>{const x=norm(field(r,k))||'Não informado';m.set(x,(m.get(x)||0)+weight(r))});return[...m].sort((a,b)=>b[1]-a[1])}function timeout(p,ms,label){return Promise.race([p,new Promise((_,rej)=>setTimeout(()=>rej(new Error('Tempo excedido em '+label)),ms))])}function stat(t){$('status').textContent=t;$('loadingText').textContent=t}async function fetchBook(path){const d=await timeout(sb.storage.from(BUCKET).download(path,{}, {cache:'no-store'}),20000,'download direto');if(!d.error&&d.data)return d.data.arrayBuffer();const s=await timeout(sb.storage.from(BUCKET).createSignedUrl(path,120),15000,'URL assinada');if(s.error||!s.data?.signedUrl)throw d.error||s.error;const r=await timeout(fetch(s.data.signedUrl+(s.data.signedUrl.includes('?')?'&':'?')+'_ts='+Date.now(),{cache:'no-store'}),20000,'transferência');if(!r.ok)throw new Error('HTTP '+r.status);return r.arrayBuffer()}async function loadRows(){sb=window.supabase.createClient(URL,KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:false}});stat('Confirmando arquivo em '+FOLDER+'...');const ls=await timeout(sb.storage.from(BUCKET).list(FOLDER,{limit:1000,offset:0,sortBy:{column:'name',order:'asc'}}),15000,'listagem');if(ls.error)throw ls.error;const file=(ls.data||[]).find(x=>x.id!==null&&norm(x.name).toUpperCase()===NAME.toUpperCase());if(!file)throw new Error(NAME+' não localizado. Encontrados: '+(ls.data||[]).map(x=>x.name).join(', '));const path=FOLDER+'/'+file.name;stat('Baixando '+path+'...');const buf=await fetchBook(path);stat('Processando planilha...');const wb=XLSX.read(buf,{type:'array',cellDates:true}),sn=wb.SheetNames.find(x=>norm(x).toUpperCase()==='EVENTOS_TT')||wb.SheetNames[0];if(!sn)throw new Error('Nenhuma aba encontrada');const out=XLSX.utils.sheet_to_json(wb.Sheets[sn],{defval:null,raw:true});if(!out.length)throw new Error('Aba '+sn+' sem registros');return out}function buildFilters(){$('filters').innerHTML=FILTERS.map(([k,l])=>`<div class="filter"><label>${l}</label><select id="f_${k}" multiple></select><small>Ctrl/Cmd para multiseleção</small></div>`).join('');FILTERS.forEach(([k])=>{const v=[...new Set(all.map(r=>norm(field(r,k))).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'pt-BR'));$('f_'+k).innerHTML=v.map(x=>`<option>${esc(x)}</option>`).join('')})}const selected=k=>[...$('f_'+k).selectedOptions].map(o=>o.value);function apply(){const s=Object.fromEntries(FILTERS.map(([k])=>[k,selected(k)]));rows=all.filter(r=>FILTERS.every(([k])=>!s[k].length||s[k].includes(norm(field(r,k)))));render()}function period(r){const d=field(r,'date');if(d){const x=new Date(d);if(!isNaN(x))return x.getFullYear()+'-'+String(x.getMonth()+1).padStart(2,'0')}const y=norm(field(r,'year')),m=norm(field(r,'month')),i=M.findIndex(x=>x.toLowerCase()===m.slice(0,3).toLowerCase());return y?(i>=0?y+'-'+String(i+1).padStart(2,'0'):y):''}function change(){const p=[...new Set(rows.map(period).filter(Boolean))].sort(),a=p.at(-1),b=p.at(-2),ca=sum(rows.filter(r=>period(r)===a)),cb=sum(rows.filter(r=>period(r)===b));return{v:cb?(ca-cb)/cb*100:0,label:b?b+' → '+a:a||'—'}}function chart(id,type,data,label,h=false){charts[id]?.destroy();charts[id]=new Chart($(id),{type,data:{labels:data.map(x=>x[0]),datasets:[{label,data:data.map(x=>x[1]),backgroundColor:type==='line'?'#DA291C':['#DA291C','#333','#F79009','#12B76A','#2E90FA','#7A5AF8'],borderColor:'#DA291C',borderWidth:type==='line'?3:0,tension:.25,fill:type==='line'}]},options:{responsive:true,maintainAspectRatio:false,indexAxis:h?'y':'x',plugins:{legend:{display:type==='line'}},scales:{y:{beginAtZero:true}}}})}function render(){const total=sum(rows),ca=count(rows,'cause'),dd=count(rows,'ddd'),ch=change();$('selectionInfo').textContent=fmt(total)+' evento(s) após os filtros';$('kpis').innerHTML=[['Total de eventos',fmt(total),'Base filtrada'],['Principal causa',ca[0]?.[0]||'—',ca[0]?pf(ca[0][1]/total*100):'—'],['Região mais afetada',dd[0]?.[0]||'—',dd[0]?fmt(dd[0][1])+' eventos':'—'],[ch.v>=0?'Crescimento':'Redução',pf(ch.v),ch.label],['Participação percentual',pf(sum(all)?total/sum(all)*100:0),'Sobre o total estadual']].map(x=>`<article class="card kpi"><div class="label">${esc(x[0])}</div><div class="value">${esc(x[1])}</div><div class="sub">${esc(x[2])}</div></article>`).join('');$('insights').innerHTML=[`Principal causa: <b>${esc(ca[0]?.[0]||'—')}</b>.`,`Top 3 causas: ${ca.slice(0,3).map(x=>esc(x[0])+' ('+fmt(x[1])+')').join(' • ')||'—'}.`,`DDD mais afetado: <b>${esc(dd[0]?.[0]||'—')}</b>.`,`A seleção representa <b>${pf(sum(all)?total/sum(all)*100:0)}</b> do total.`,`Variação: <b>${pf(ch.v)}</b> (${esc(ch.label)}).`,`Fonte: <b>${BUCKET}/${PATH}</b>.`].map(x=>`<div class="insight">${x}</div>`).join('');chart('yearChart','bar',count(rows,'year').sort((a,b)=>Number(a[0])-Number(b[0])),'Eventos');chart('semesterChart','bar',count(rows,'semester').sort(),'Eventos');let mo=count(rows,'month');mo.sort((a,b)=>M.indexOf(a[0].slice(0,3))-M.indexOf(b[0].slice(0,3)));chart('monthChart','bar',mo,'Eventos');chart('causeChart','bar',ca.slice(0,12),'Eventos',true);chart('dddChart','bar',dd,'Eventos');const tm=new Map();rows.forEach(r=>{const k=period(r);if(k)tm.set(k,(tm.get(k)||0)+weight(r))});chart('trendChart','line',[...tm].sort((a,b)=>a[0].localeCompare(b[0])),'Eventos');heat(ca,dd);compare(dd);annual()}function heat(ca,dd){const ds=dd.slice(0,12).map(x=>x[0]),cs=ca.slice(0,10).map(x=>x[0]);let mx=0,g=ds.map(d=>cs.map(c=>{const n=sum(rows.filter(r=>norm(field(r,'ddd'))===d&&norm(field(r,'cause'))===c));mx=Math.max(mx,n);return n}));$('heatmap').innerHTML=`<table class="heat"><tr><th>DDD</th>${cs.map(c=>`<th>${esc(c.slice(0,14))}</th>`).join('')}</tr>${ds.map((d,i)=>`<tr><th>${esc(d)}</th>${g[i].map(n=>`<td style="background:rgba(218,41,28,${mx?.12+.88*n/mx:.06});color:${mx&&n/mx>.5?'#fff':'#333'}">${fmt(n)}</td>`).join('')}</tr>`).join('')}</table>`}function card(t,d,a){const c=count(a,'cause');return`<article class="card"><h3>${t}</h3><div class="metric"><span>DDD</span><b>${esc(d||'—')}</b></div><div class="metric"><span>Eventos</span><b>${fmt(sum(a))}</b></div><div class="metric"><span>Principal causa</span><b>${esc(c[0]?.[0]||'—')}</b></div><div class="metric"><span>Top 3</span><b>${c.slice(0,3).map(x=>esc(x[0])).join(', ')}</b></div></article>`}function compare(dd){const chosen=selected('ddd'),focus=chosen.length===1?chosen[0]:dd[0]?.[0],worst=count(all,'ddd')[0]?.[0],fr=rows.filter(r=>norm(field(r,'ddd'))===focus),wr=all.filter(r=>norm(field(r,'ddd'))===worst);$('comparison').innerHTML=card('Área filtrada',focus,fr)+card('Área mais crítica do RS',worst,wr)}function annual(){const ys=[...new Set(all.map(r=>norm(field(r,'year'))).filter(Boolean))].sort();$('annual').innerHTML=ys.map(y=>{const a=rows.filter(r=>norm(field(r,'year'))===y),c=count(a,'cause')[0],d=count(a,'ddd')[0];return`<article class="card"><h3>${y}</h3><div class="metric"><span>Total eventos</span><b>${fmt(sum(a))}</b></div><div class="metric"><span>Principal causa</span><b>${esc(c?.[0]||'—')}</b></div><div class="metric"><span>DDD mais afetado</span><b>${esc(d?.[0]||'—')}</b></div></article>`}).join('')}async function init(){try{all=await loadRows();rows=[...all];buildFilters();render();$('loading').classList.add('hidden');$('app').classList.remove('hidden');$('status').textContent=fmt(sum(all))+' eventos • '+BUCKET+'/'+PATH;$('apply').onclick=apply;$('clear').onclick=()=>{FILTERS.forEach(([k])=>[...$('f_'+k).options].forEach(o=>o.selected=false));apply()};try{parent.postMessage({source:'portal-panel',type:'ok',page:'backbone_analytics.html',text:'Base carregada'},location.origin)}catch(_){}}catch(e){$('loading').classList.add('hidden');$('error').classList.remove('hidden');$('error').innerHTML='<b>Não foi possível carregar a base.</b><br>'+esc(e.message)+'<br><small>Fonte esperada: '+BUCKET+'/'+PATH+'</small>';$('status').textContent='Falha: '+e.message;console.error(e)}}init()})();
